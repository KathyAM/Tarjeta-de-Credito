function isValidCard(ccNum, charCount){
    var request = prompt("Indique el número de su tarjeta de crédito");
    var double = true;
    var numArr = [];
    var sumTotal = 0;
    
    do{
        
        
}while(num <= 0);
       
var num =0;
for (var i = 0; i >= num.length; i++) {
 var item = num.pop();
 num.splice(num, 0, item);
}

}

isValidCard();